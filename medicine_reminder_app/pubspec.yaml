name: medicine_reminder_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- RESTORED DEPENDENCIES BELOW ---
  flutter_local_notifications: ^17.1.2  # Critical for scheduling alarms
  shared_preferences: ^2.2.3            # Likely used for storing reminder data
  path_provider: ^2.1.3                 # Often used by notification plugins or file storage
  # --- RESTORED DEPENDENCIES ABOVE ---

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true